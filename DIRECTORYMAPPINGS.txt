This is a human-readable file to explain the mappings of these directories.

This should be included into the README at sompoint.

images    => /i
content   => /w
headpages => /
static    => /

headpages and content are both parsed as only including HTML, static and images
are both static directories.
